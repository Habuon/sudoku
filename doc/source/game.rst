Game
====

Game class is supposed to unite all other classes into one working entity.

It has a run function which starts the game.

When the run function is started Game uses Print class and its functions to ask user for input and standard input for getting
input form user. The game class has its own overloaded operator << which is used for getting an input from user.


